/**
 * @format
 */

import { registerRootComponent } from 'expo';
import App from './App'; // Make sure the path to App.tsx is correct

// Register the root component of the app
registerRootComponent(App);
